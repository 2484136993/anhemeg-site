# 上海安禾美阁静态网站部署指南

## 部署包位置
- `anhemeg-site-dist.tar.gz` - 完整网站压缩包

## 方案一：Vercel 部署（推荐）

### 步骤 1：解压文件
```bash
tar -xzvf anhemeg-site-dist.tar.gz
cd anhemeg-site
```

### 步骤 2：安装 Vercel CLI 并登录
```bash
npm install -g vercel
vercel login
```

### 步骤 3：部署到 Vercel
```bash
vercel
```
按提示选择项目配置，最后输入 `y` 确认部署。

### 步骤 4：正式上线
```bash
vercel --prod
```

### 步骤 5：绑定域名 anhemeg.top
1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 选择 anhemeg-top 项目
3. 进入 Settings → Domains
4. 添加域名：`anhemeg.top`
5. 按照页面提示配置 DNS 记录

---

## 方案二：GitHub + Vercel 自动部署

### 步骤 1：创建 GitHub 仓库
1. 登录 GitHub，点击右上角 + → New repository
2. 仓库名称：`anhemeg-site`
3. 选择 Private（私有）或 Public（公开）
4. 点击 Create repository

### 步骤 2：上传代码
```bash
# 在 anhemeg-site 目录下执行
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/anhemeg-site.git
git push -u origin main
```

### 步骤 3：连接 Vercel
1. 登录 [Vercel](https://vercel.com)
2. 点击 "New Project"
3. 选择 "Import Git Repository"
4. 选择刚创建的仓库
5. Framework Preset 选择 "Other"
6. 点击 "Deploy"

### 步骤 4：绑定域名
同上步骤 5

---

## 方案三：Netlify 部署（无需 GitHub）

### 步骤 1：注册 Netlify
访问 https://app.netlify.com/drop 注册（可用 GitHub/Google/Email）

### 步骤 2：拖拽部署
1. 解压 anhemeg-site-dist.tar.gz
2. 打开 https://app.netlify.com/drop
3. 直接拖拽 `anhemeg-site` 文件夹到页面
4. 自动生成临时预览 URL

### 步骤 3：绑定域名
1. 点击生成的 Site settings
2. 选择 Domain management → Add custom domain
3. 输入 `anhemeg.top`
4. 配置 DNS 记录（见 DNS 配置指南）

---

## 部署验证
部署完成后访问以下地址确认：
- Vercel: `https://anhemeg-site.vercel.app`
- Netlify: `https://xxx.netlify.app`

验证页面：
- 首页：http://preview-url/
- 医生页：http://preview-url/doctors.html
- 地图：http://preview-url/sitemap.xml

---

## 注意事项
1. 确保 vercel.json 配置文件已包含在项目中
2. 域名解析可能需要 24-48 小时生效
3. HTTPS 证书由平台自动申请
